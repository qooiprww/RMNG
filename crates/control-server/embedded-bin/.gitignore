# Dev payload dir: stage clone-daemon.gz / agent-wrapper.gz / gnome-shell-deb.gz here
# for `cargo run -p control-server` (assets.rs reads this dir when the image install
# path /usr/local/share/rmng doesn't exist). The Docker image does NOT use this dir.
*
!.gitignore
